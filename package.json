{
  "name": "cdai",
  "private": true,
  "packageManager": "npm@11.12.1",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "npm run test --workspace=apps/web && npm run test --workspace=packages/ui && npm run test --workspace=packages/theme && npm run test --workspace=packages/types",
    "test:turbo": "turbo test",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
