# Populate clinician-approved native-Arabic exemplars/seeds + mirror-when-marked wiring

Branch: `feat/shadow-historical-replay` (worktree only, no branch switch)

## Commits

1. `30ea889` feat(shadow): populate clinician-approved khaleeji exemplars, bump to v1.0.0
   - `src/sage_poc/prompts/khaleeji_shadow_exemplars.json`: new schema
     `{en, ar_m, ar_f, ar_neutral}`, version `1.0.0` (off `-draft`), clinician-approved
     2026-07-08 note, both exemplars filled exactly per spec. Emirati feminine ك→ج
     (عليج/وياج) preserved verbatim.
   - `src/sage_poc/prompts/loader.py`: `load_khaleeji_shadow_exemplars(gender="none")`
     now selects the rendering (`f`->`ar_f`, `m`->`ar_m`, anything else->`ar_neutral`);
     existing no-arg callers (`shadow_arabic.py`, `replay_driver.py`) keep working and
     get the safe neutral default.
   - Tests: `tests/test_khaleeji_exemplars_loader.py` (8 tests, rewritten) — schema shape,
     version, per-rendering selection, ك→ج preservation, default fallback.

2. `3117642` feat(shadow): wire mirror-when-marked gender policy into shadow generation
   - `src/sage_poc/prompts/composer.py` (`compose_prompt`, `shadow_arabic=True` block):
     computes `gender = detect_gender_marking(state.get("raw_message") or "")`
     (`scripts/register_eval/gender_marker.py`, deferred import), selects the matching
     exemplar rendering via `load_khaleeji_shadow_exemplars(gender)`, and appends the
     mirror-when-marked directive: "Address the user in the gender they grammatically
     self-mark; if unmarked, use gender-neutral constructions. Never infer gender from
     topic or name." Scoped to the shadow path only (`shadow_arabic=True`); the
     translate-out path (`shadow_arabic=False`, live serving) is untouched.
   - Cross-package import note: `sage_poc.prompts.composer` (src/) now imports from
     `scripts.register_eval.gender_marker` (repo-root script tree), mirroring the
     existing reverse direction (`replay_driver.py` already imports from `sage_poc`).
     This works under `pythonpath = ["src", "."]` (pyproject) and in scripts run from
     repo root. If it's ever unimportable in some deployment context, the failure is
     swallowed by `shadow_arabic.py`'s existing fail-open `try/except` (never touches
     live serving) — consistent with the "measurement only, fail-open" contract. Flagged
     for reconciliation alongside the PR #220 promotion of `gender_marker.py` to
     `src/sage_poc/`.
   - Tests: `tests/test_composer_shadow_arabic.py` (+6 tests) — directive present,
     unmarked -> ar_neutral (no ar_m/ar_f leakage), feminine-marked ("أنا تعبانة اليوم")
     -> ar_f + عليج/وياج present + no ar_m leakage, masculine-marked ("أنا تعبان اليوم")
     -> ar_m + no ar_f leakage, directive absent from the non-shadow (`shadow_arabic=False`)
     path.

3. `7ac681c` feat(shadow): fill 3 clinician-authored seed inputs (016, 024, 025)
   - `scripts/register_eval/seed_inputs.json`: seed-016 (khaleeji workplace overwhelm),
     seed-024 (arabizi venting fragment), seed-025 (khaleeji MSA->dialect drift) filled
     with the exact clinician-approved text, `source` set to
     `clinician_authored_2026-07-08`, `id`/`lang_profile`/`intent_en` preserved. File
     `note` field updated to reflect the new 15/3/7 split (was stale at 15/10). The
     other 7 `PLACEHOLDER_NATIVE_AUTHOR` slots (017-023) are untouched — out of scope
     for this pass.
   - Tests: `tests/test_seed_inputs.py` (new, 5 tests) — text/source/lang_profile match
     exactly, no `PLACEHOLDER_NATIVE_AUTHOR` on the three target ids,
     id/intent_en preserved, `_load_seed_items()` now includes all three (it skips
     empty-text entries), and the remaining 7 placeholders are confirmed still untouched.

## Test results

All new/touched tests green:
- `tests/test_khaleeji_exemplars_loader.py`: 8/8 passed (rewritten; was 1 test)
- `tests/test_composer_shadow_arabic.py`: 8/8 passed (3 original + 5 new)
- `tests/test_seed_inputs.py`: 5/5 passed (new file)
- `tests/test_shadow_arabic.py`, `tests/test_replay_driver.py`, `tests/test_shadow_eval_row.py`, `tests/test_gender_marker.py`: all still green (25/25 combined re-run after Task 2)

Full-suite regression check (`uv run pytest tests/`): 201 failed / 2738 passed on this
branch. Verified via a disposable `git worktree` at the pre-change commit (`06c0699`):
baseline is **also** 201 failed / 2721 passed (the delta of 17 extra passes on our
branch = new tests we added). All 201 failures are pre-existing sandbox-environment
issues (missing `OPENAI_API_KEY`/`OPENROUTER_API_KEY`, no network) unrelated to this
change — confirmed by diffing the failing-test names between baseline and post-change
runs (identical sets). No regressions introduced.

One pre-existing failure worth flagging by name since it's in the shadow-measure
family: `tests/test_shadow_never_served.py::test_sentinel_never_in_served_response`
fails on both baseline and this branch with `openai.OpenAIError: Missing credentials`
— an environment gap (no API key in sandbox), not a regression from this work.

## Task 4 — Layer 1 attempt

Command run (positional `source` arg per the driver's actual CLI, not `--source`):

```
uv run python -m scripts.register_eval.replay_driver seed --run-id layer1-shakedown-20260709 --exemplar-version 1.0.0 --limit 3
```

**Result: did not run — no Supabase credentials in this sandbox.** Neither
`SUPABASE_URL` nor `SUPABASE_SERVICE_KEY` is set (no `.env` file present, only
`.env.example`), so `ReplayClient.__init__` leaves `self._url = None`. The very first
DB read (`fetch_done_source_ids`, called before any translate/generate/LLM call) fails
immediately with:

```
httpx.UnsupportedProtocol: Request URL is missing an 'http://' or 'https://' protocol.
```

Full traceback bottoms out at `replay_driver.py:117` in `fetch_done_source_ids` ->
`http.get(f"{self._url}/rest/v1/shadow_register_eval", ...)` with `self._url=None`.
This is a pre-LLM failure (never reached OpenAI/OpenRouter) — purely missing Supabase
config in this environment. The exemplars/seeds/wiring are otherwise ready; the run
should be attempted from an environment with `SUPABASE_URL` + `SUPABASE_SERVICE_KEY`
(and an LLM key for the `generate` step) set.

## Concerns / follow-ups

- Cross-package import direction (`sage_poc` -> `scripts.register_eval`) is new; flagged
  above for reconciliation with PR #220's promotion of `gender_marker.py` to
  `src/sage_poc/`.
- Layer 1 was not actually exercised end-to-end (no live LLM call happened) because it
  never got past the Supabase credential check — so the generation code path (compose
  ->  gender detection -> rendering selection -> LLM call) is verified only by unit
  tests with fakes, not by a real run in this session.
