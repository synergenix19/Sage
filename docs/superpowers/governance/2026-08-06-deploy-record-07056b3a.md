# Deploy record — 07056b3a (2026-08-06): family deroute live, chain terminated

**Authorization:** PO execution directive 2026-08-06 ("execute option (a) — deroute now,
with the SPV ruling"); SPV ruled in by its deciding fact (signature record not found,
verified — deroute record 2026-08-06-body-scan-spv-deroute-record.md).

**Payload (`5434dedc..07056b3a`):** PR#409 (body_scan + SPV deroute + blurb removals +
behavior pin) + PR#407 (decision packet + deroute checklist) + PR#408 (grief deference
BUILT INERT, flag OFF, byte-identical — NOT activated) + #405/#406 records already
serving-neutral. Serving delta = exactly the two deroutes.

## Cycle

1. Pre-verify: watchdog CLEAN (52 flags, three-way).
2. deploy_prod.sh: lock, ancestry 6/6, cache-bust, step-5 apply `already converged`.
3. railway up → converged `07056b3a`; readback truthful (consult true, crisis templated).
4. Drift probe: CLEAN (52 flags).
5. Smoke --tier all: ALL must-pass green. **The regime-aware MM check's live probe now
   shows `skill-id=''` on the explicit meditation request — before this deploy it showed
   `mindfulness_body_scan`. The absorption is gone; the §1a-§6 family chain is measured-
   terminated in serving, not just in analysis.** Tier-B skip explicit (storage-state
   still the pending human step).
6. Lock released (singular delete); post-release readback stable.

## Rollback

Redeploy `5434dedc` (restores body_scan + SPV routability — only under a superseding
clinical ruling; the register and skip-set are the front door).

## Open on the clinical sheet (unchanged by this deploy)

Vee section B sentence = confirm-or-reverse per skill (re-route staged, one line each);
A1 (EMR gate, no fallback); A2/A3 pins; A4 name-or-nobody → A5; C1-C3 grief ticks (guard
stays OFF until her tick); D scheduling.
