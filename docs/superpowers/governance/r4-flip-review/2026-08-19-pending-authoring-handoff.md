# PENDING — the sitting's authoring half (new worksheet picks up here)

**Status 2026-08-19 (owner: "note as pending; new worksheet"):** all sitting TICKS are
ratified (`2026-08-19-sitting-ticks-record.md`) and every authorized execution is landed
(#507, #508, feat `8ebe6809`). What remains is exclusively **Vee's authoring**, then the
signature, then the pre-registered mechanical sequence. Nothing engineering-side blocks
or proceeds until then.

## Pending items (all hers; engineering scaffolds are ready and linked)

1. **A4 — Gulf Arabic passive-SI list:** approve/edit the 10-candidate research draft
   (`2026-08-19-gulf-arabic-passive-si-research-draft.md`; note the SK-AR-006 dedup flag
   on #9). Approved entries → v2 `sf1_gulf_arabic_passive_si` slot.
2. **A5 — her 1–2 transient-presenter phrases** ("since X last week" shape) → the
   stage-2 fixture set (`low_mood_3a_stage2_transients.json`, feat branch). This is
   **F5 gate condition (2)** — the strict ≥0.90/≤0.00 gate stands until it is signed.
3. **B1a — intent-now boundary vocabulary** (EN + Gulf Arabic): what in a helper's
   report counts as plan/means/intent-now. Variant TEXT already drafted for her
   signature (ticks record); tie-break already pinned (ambiguous immediacy → 999-led).
4. **B3 — assent-class + ideation-echo vocabulary** (EN + AR "وأنا بعد" tails): both
   fix-before-flip per her tick; the kin-parity half is already landed (#508).
5. **B2 — native `ar_uae` twins:** base helper content + both variants (B1a/B1b). The
   locale-parity boot guard makes activation impossible without them.
6. **A6 — the v2 signature block** (signature fields PENDING in the file; trigger text
   already confirmed). Signing = version, date, the A2 trigger, manifest pins.

## What fires automatically after A6 + the signed pieces (pre-registered, no decisions)

v2 baseline on the pre-F5 detector (prediction stated first, per R7) → F5 build →
measurement through the fixed harness only, against the pre-registered gate
(`2026-08-18-f5-gate-preregistration.md`; condition 1 satisfied, condition 2 = item 2
above) → R4 flip in its own predeclared window
(`2026-08-18-flip-predeclaration.md`, three amendments; rollback = the flag).

## Standing rules the new worksheet inherits

Ratification-transport rule (`2026-08-19-ratification-transport-rule.md`): commit her
answers to this directory BEFORE executing them; conditions ride PR descriptions. The
scenario set freezes as the permanent R4 regression fixtures on completion (B5). The
living review record: `sage-poc` untracked `docs/code_review.md` + the owner's artifact.
