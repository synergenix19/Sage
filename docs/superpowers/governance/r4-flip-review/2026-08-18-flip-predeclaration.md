# R4 flip predeclaration — DRAFT (declared before the flip window; owner-directed 2026-08-18)

**Flip prerequisites (all four, none waivable):**
1. Vee's signature on the EN content (`CC-EN-TP-001`, with the R2 signature block) after her
   scripted E2E transcript review — the signed JSON and the rendered conversation are
   different artifacts; she signs having seen what a user sees (S-5 naturalness applied to
   her own content; the T-11 lesson).
2. A natively-authored `ar_uae` twin, signed (Khaleeji lane; the locale-parity boot guard
   makes EN-only activation structurally impossible).
3. Her answers to the two flagged clinical questions (helper-turn monitoring semantics;
   crisis-card display).
4. The adversarial boundary pass clean, or its findings dispositioned (any slip-through is
   a Layer-1 lexicon finding independent of R4, fixed first).

**Predeclared metric expectations:**
- Third-party turns shift from the acute script to helper content — the intended effect.
- **Crisis-route rate UNCHANGED** — deference is content-selection, not routing; if the
  rate moves, something changed that should not have. This is the anomaly tripwire.

**Post-flip verification (prod's own audit rows, the F1 standard):**
- A third-party turn attributable as Layer-1-clean + deference-served (audit:
  `crisis_flags: []` + `third_party_crisis: true` + the helper content delivered).
- A compound probe confirming the first-person script still wins (both orders).
- Boot-log census + register parity stamp, per the standing window checklist.

**Rollback unit: the flag itself** (`SAGE_THIRD_PARTY_DEFERENCE` → register PR + apply,
instant-off) — the cleanest rollback in the project.

**Fixture permanence:** the reviewed transcript scenario set becomes the permanent R4
regression fixture set — written once, kept forever.

**Amendment (post-adversarial-pass, 2026-08-18):** prerequisite 4 now has named content —
the three Layer-1 findings in the review sheet (assent-class coverage, SK-EN-004 kin
variants, ideation-echo class) must be dispositioned by Vee (fixed, or interim risk
accepted in writing) before the flip. Design question 3 (monitoring × third-party) joins
the two standing clinical questions in prerequisite 3.
