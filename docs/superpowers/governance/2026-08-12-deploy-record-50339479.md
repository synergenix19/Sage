# Deploy record — 50339479 (2026-08-12): EMR LIVE (register change #6)

**Authorization chain:** Vee lexicon sign-off (blobs pinned,
`2026-08-12-emr-lexicon-signoff-and-flip.md`) + PO flip authorization in-session +
Phase-3 evidence basis (deltas report: drop 28/30 -> 0/30, loop first-line 1.00 x30,
controls neutral).

**Payload (`07056b3a..50339479`, src-only delta = EMR end-to-end):** deterministic
detector (44-entry signed lexicon), signed adaptive screen (screen-first, duration
mandatory), three consumers (executor exit-and-rehand, info_request early-return,
offer-reply Option A with release-never-declines), resumption hold, readback field.

## Cycle

1. Register change #6 merged (PR#434): `SAGE_MODALITY_REQUEST_ROUTING` null -> "true",
   signed_value + signature_ref.
2. deploy_prod.sh: lock, ancestry 6/6, cache-bust, **step-5 apply's FIRST ACTIVATING
   use** — detected committed-true vs desired-unset, one batched set, pre-build.
3. `railway up` -> build `9dff1649`. **Verification incident, caught by the ladder:**
   the SHA-based convergence poll fired EARLY — the flag-apply restart served the OLD
   image with the new `RAILWAY_GIT_COMMIT_SHA` env, so `build_sha` lied (the runbook's
   "necessary-not-sufficient" warning, live instance #2). The readback-SHAPE check
   (EMR field absent) caught it; convergence was re-polled on the BEHAVIORAL signal
   (field present AND true). Runbook lesson reaffirmed: converge on behavior, never on
   the env-derived SHA.
4. Readback: `modality_request_routing_enabled: true`, raw_env "true", consult true,
   crisis templated. Drift probe: CLEAN, 53 flags.
5. Smoke `--tier all`: ALL must-pass green (crisis EN+AR, helpline, MM deroute regime,
   precedence, VCS guard, readbacks; Tier-B skip explicit).
6. **Live request-family probes (window 1), verbatim results:**
   - "are there any exercises i can do" -> Vee's signed screen copy served verbatim
     (`gate_path screen`, full EMR path markers) — the original defect turn, fixed in
     serving.
   - Answer supplying onset+duration -> §1a first-line pair offered (box breathing +
     5-4-3-2-1), `modality_request_routed:select > skill_offer_made`.
   - Benign + crisis controls: unchanged (smoke).
7. Lock released; post-release stability confirmed on the behavioral signal.

## Window 2 — VERIFIED (2026-08-12 evening, deploy-owner-directed)

Re-verification run at deploy-owner direction, separated from window 1 by several
hours and TWO service restarts (the lock-release restart + the flag-apply restart:
distinct containers, distinct provider sessions — the provider-rotation surface the
window-bounded rule exists to test). Results: drift probe CLEAN (53 flags);
live functional suite 10/10 — including the mid-skill affirmation check that was
single-sample-variant in window 1, now passing cleanly (skill held, step advanced).

Separation-standard note (honest): this record originally pinned window 2 to "a
separate day"; the deploy owner directed the re-verification same-day and the
hours+two-restart separation satisfies the rule's intent (backend-mix rotation
between windows). **The EMR served-stability claim is BOUND as of this run.** A
further calendar-day spot-check remains free and welcome but is no longer owed.

## Rollback

`value: "false"` via register PR + apply (instant-off, byte-identical) — code can stay.
