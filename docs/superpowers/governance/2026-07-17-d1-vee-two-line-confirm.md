# To Vee — two lines, the only human dependency left on the D1 flip path

**Out today (not "before flip" abstractly): the shadow window is accruing, serve/resume is days not weeks,
and these two confirms are the last human gate on the flip.** Neither blocks the shadow window (running now,
serves nothing, reads no answer rows). Both block the FLIP.

## Line 1 — the served question bytes (comma-swap confirm)
At sign-off you approved the A1 wording; engineering applied one pre-freeze convention fix — the em-dash
became a comma, per the standing no-em-dash-in-mirrorable-content rule ("Before we try this one **—** quick
check" → "Before we try this one**,** a quick check"). Every clinical marker and both beats are byte-identical
otherwise. These are the frozen, manifest-pinned bytes that will reach a user at flip (the serve/resume
terminal node emits them verbatim, hash-checked against the manifest):

> *"Before we try this one, a quick check first: does anything about how this feels seem different from your
> usual anxiety? Like a sharp or crushing pain rather than tightness, pain spreading to your arm, jaw, or
> back, real trouble breathing rather than shallow breathing, or numbness or weakness on one side? And is
> there anything like a heart condition, or a pregnancy, that I should know about before we try it?"*

▢ **confirmed — these exact bytes**   ▢ change: ______

## Line 2 — the RULING 3 gate-split (placement, not thresholds)
Your RULING 3 thresholds are **unchanged in number**. One honest placement correction: a **silent** safety
shadow measures **fire-volume only** — it cannot read the answer-class distribution (unclear / evaded /
clear_no / contraindication_disclosed rates), because those need the question *served and answered*, which
silent shadow by design does not do. So the criteria split by which gate reads which row:
- **Shadow-window gate:** fire-volume / would-fire rate + disclosure-population (measurable now).
- **Post-flip monitored-enforce gate:** the answer-class distribution (measurable only once serving is live,
  fail-safe actually protecting users). If `unclear` dominates → wording returns to you before "verified." If
  a zero-tolerance row fires (crisis-in-answer mishandled, audit swallow) → enforce halts back to shadow.

▢ **confirmed — split as stated, thresholds unchanged**   ▢ change: ______

*Both confirmed → the flip has zero remaining human dependencies; it proceeds on shadow's self-reading
fire-volume criteria once the pre-registered N is met. Records: 2026-07-17-d1-vee-signoff-V.md (bytes),
2026-07-17-d1-shadow-window-criteria.md (split + N).*
