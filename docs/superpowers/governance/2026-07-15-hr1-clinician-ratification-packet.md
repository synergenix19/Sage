# HR-1 Stage 1 — Clinician ratification packet (approve / amend / reject)

**What this gates:** the flip that makes SageAI detect high-risk psychiatric presentations (psychosis, mania, dissociation) and route them to the professional referral instead of offering a self-help skill. The code is built, flag-gated OFF, and fully tested; it does nothing in production until you ratify and we flip it. **Every trigger phrase below is copied verbatim from BOT BEHAVIOUR §HR.0** — your task is to confirm the doc's own table (and the boundary controls), not to author new content.

## ★ RECOMMENDATION (approve / edit / reject this)
> **Ratify the §HR.0 trigger table as the detector's phrase list, and flip HR detection ON.** It closes a live safety miss: today a manic user is offered a breathing exercise and a dissociating user is offered grounding — both of which the doc separately marks as *contraindicated* for these states. The detected-hallucination case already refers correctly; this extends that same working referral to the other high-risk presentations. **Two items below need your judgment (not a rubber-stamp); one question needs your ruling.**

## What you are ratifying
The detector fires on these verbatim §HR.0 phrases (+ minimum word-order variants), setting a clinical flag that routes to the existing professional referral:
- **Psychosis (15 phrases)** — "people are following me", "someone is watching me", "people are trying to hurt me", "I don't think I can trust anyone", "I hear voices", "I hear people talking when no one is there", "the voices won't stop", "I see things other people don't see", "I think people are reading my thoughts", "I think someone is controlling my thoughts", "the TV is sending me messages", "everything feels like a sign", "I don't know what's real anymore", **"I think everyone is against me"**, "I feel detached from reality". *(15 phrases, transcribed verbatim from the doc's unnumbered §HR.0 bullet list — the doc gives no count; we verify by name, and an earlier internal count of ours had dropped one, now restored.)*
- **Mania (10 phrases)** — "I haven't slept for days but I feel amazing", "I don't need sleep", "my mind is going a million miles an hour", "I have so much energy", "I feel unstoppable", "I feel invincible", "I'm taking huge risks", "I've been spending loads of money", "I keep talking and can't stop", "my thoughts are racing all the time".
- **Dissociation (11 phrases)** — "I don't feel real", "nothing feels real", "I feel detached from myself", "I feel outside my body", "I feel like I'm watching myself", "the world doesn't feel real", "everything feels like a dream", "I lose chunks of time", "I feel disconnected from reality", "I feel completely numb and absent", "I don't know where I am sometimes".

And the **must-NOT-fire boundary** — these deliberately do NOT route to high-risk (confirm the boundary is right): grief ("I feel numb since he died", "nothing's felt real since the funeral"), panic-derealization ("everything felt unreal during the panic attack"), idiom/good-news ("this promotion doesn't feel real"), ordinary excitement ("I'm so energized about this project").

## Two items that need your judgment
1. **Mania "I have so much energy" — a known recall gap.** This bare phrase is a literal substring of the benign control "I have so much energy today after the gym," so no keyword can catch the bare phrase without also firing on someone who's just energized. We chose **precision over recall**: the phrase only fires when it co-occurs with another mania marker (e.g. "...and I haven't slept in days"). **Confirm this trade is acceptable**, or tell us to accept the false-positives on bare energy language.
2. **Dissociation at the referral tier.** "Nothing feels real" spans panic-adjacent derealization (lower acuity) to psychotic dissociation (high). The doc routes all dissociation to referral. The false-positive cost is a referral surfaced to a panicking user; the false-negative is a dissociating-from-psychosis user handed grounding. **Confirm dissociation belongs at the referral tier** (our default, per doc), or specify a lower tier for a subset.

## Stage 2 forward-item (blocker-with-default — sign the default now, same conversation)
The doc's §3 escalates on the distress-0-10 answer (high/agitation/risk-underway → 999/ER now; low → see a doctor promptly). Users often reply with **content instead of a number** ("they're outside right now") — in fact that is the *modal* reply for someone mid-crisis. The default the terminal will use:
- **Risk/agitation language in the reply bypasses everything** → straight to the higher-severity (999/ER) branch. A reply like "they're outside right now, I can't stay here" is already the §3 evidence in words; we never re-ask someone who has given it.
- **Otherwise, no parseable score →** one **gentle re-ask** (the same distress question, once — not probing content, so doc-compliant), then if still no score, **default to the higher-severity branch**. Fail toward emergency, never toward see-someone-soon.

**Confirm this default** (risk-language → 999 immediately; else one re-ask, then fail-to-higher). (Stage 2 build; surfaced here so it lands in one conversation.)

**One clinical parameter in the same §3 decision:** the doc escalates "high distress" → 999 but gives no number. We operationalize "high" as **distress ≥ 7 / 10 → higher-severity (999); ≤ 6 → see-a-doctor promptly.** **Confirm 7, or set the boundary.** (This only decides the *framing* for a user who gives a low number with no risk/behavior signs; 999/ER is always still offered as the escalation option.)

**And the number is NOT the only escalation trigger** — per §3's actual wording ("high distress, signs of agitation, danger, or **risky behavior already underway** — *mania-driven spending/risk-taking, any safety concern*"): **mania behavior-underway phrases (e.g. "I've been spending loads of money", "I'm taking huge risks") escalate to higher-severity regardless of the reported score.** A manic user in euphoria will self-report *low* distress while actively spending; §5 explicitly warns against being swept into that manic framing, so a low self-reported score during active-behavior mania must NOT decide "see someone soon." **Confirm — this is the doc's own §3 default.**

**Re-engagement (blocker-with-default — set to the doc's default, not the convenient one):** §HR says the protocol "takes priority over whatever category the conversation was in before qualifying content appeared, the same way crisis category does" — so *qualifying content re-engages*, like crisis (which, as noted, always fires). **Default = one-shot per HR class per disclosure-episode:** a NEW, distinct HR presentation later in the session (paranoia at turn 3, referral delivered, then mania-with-spending at turn 12) **re-engages** the protocol; the *same* disclosure merely repeated ("the voices won't stop" at turn 5, again at turn 6) gets a brief fixed-copy **reaffirm** of the referral (one line, no two-turn re-run — that repeat-loop is the only thing strict one-shot legitimately prevents). **And any behavior-underway content (spending / risk-taking) always re-engages** regardless of a prior referral — §3's escalate-regardless evidence must not be swallowed by a session bit set nine turns earlier. **Amendment option:** strict once-per-session (simpler, but it suppresses a higher-acuity later disclosure — the case above). Crisis unaffected either way. *(Note: the current Stage-2 code ships the strict-once behavior; it will be updated to the ratified default before the flip — a flip-gate task, tracked.)*

**One phrase-class to ratify-or-extend (same as the trigger tables):** §3 also escalates to 999 on **risk/agitation language in the reply** — currently a short INTERIM list ("they're outside right now", "I can't stay here", "I'm not safe"). It is authored the same way as the CF trigger phrases and carries your same authority. **Ratify these and/or extend the list.** Incompleteness here is safe-thin: a missed phrase falls through to the fail-to-higher default (over-escalation to 999) rather than to harm, and genuine danger is also caught by the always-on crisis screen.

## One ruling (blocker-with-default — the doc has already answered; you are ratifying or amending it)
The doc's §HR section names dissociation as one of its three high-risk classes with 11 triggers — so **the default is: dissociation routes to professional referral, per the doc.** You are not deciding this fresh. The genuine question is whether you want to **amend** the doc's answer — e.g. split panic-derealization to a lower tier — not to re-derive settled content. **Ratify the doc's default, or specify the amendment.**

## What happens between your ratification and Stage 2 (sign knowing this)
On flip, all five high-risk classes route to the **existing referral, which is LLM-rendered** — the doc grades this as non-conforming to §HR: it has no fixed standardized copy and does not ask the distress-0-10 question. You are ratifying **detection into an imperfect terminal.** This is a deliberate, honest trade: the contraindicated-skill harm (breathing to a manic user, grounding to a dissociating user) stops *immediately*, and delivery conformance — the doc's fixed §2 message + the §1 distress question — follows in **Stage 2, a committed build immediately behind this one.** You sign the detection now; the terminal upgrade is already scheduled, not aspirational.

## Scope / what this does NOT cover (stated, not hidden)
- **Arabic: zero coverage.** These are English triggers; Khaleeji renderings + Arabic must-NOT-fire controls go to the AR probe. This ships English-first with the AR gap on the record (same pattern as the B1 medical guard).
- **Keyword-only.** Naturalistic disclosures that don't contain a listed phrase won't fire (the #65-class limitation). Semantic detection is future work.
- **The referral copy is the current LLM-rendered one.** Stage 2 replaces it with the doc's fixed standardized message + the distress-0-10 question — a separate build immediately behind this.

## On your approval we (engineering) then:
Your ratification produces **ONE flip PR, landed atomically**, containing both: (1) CF-007/008/009 → `active:true` + your sign-off (`approved_by`), and (2) **your co-sign of the `safety_rule_activation_map`** (the activation-state pin — it was re-signed by engineering for the inactive-add, but flipping these rules to active is a *clinical* event and re-signs it under your authority). We then reconcile the signed-fields manifest and set `SAGE_HIGH_RISK_DETECTION=true` in prod, with a live post-flip verification of the four drives (B1 pattern). **Clinical authority and the prod-state change land together — never "ratify now, flip later"**; that gap between authority-granted and state-changed is exactly where #270-class drift breeds, and we just spent a day closing that class.

## Stage 2 — voice variant pools (ratify per slot, per variant)
The HR terminal's responses are **fixed, deterministic copy** (no runtime LLM — that's the safety architecture), but to avoid sounding canned they are drawn from small **pools of on-voice variants**, LLM-drafted offline in SageAI's persona and selected deterministically (a session-ID hash — reproducible for audit, non-repeating for the user). **You ratify each string** (sign / edit / reject). The annotated candidates (5 slots, 3–4 variants each, with the §5-safety rationale per variant) are in `docs/superpowers/drafts/2026-07-16-hr-copy-variants-candidates.md` and committed DRAFT/unratified in `src/sage_poc/safety/hr_copy.py`; nothing goes live until signed.

What you are signing, and the guards on it:
- **Slot 3 (the 999 redirect) is the one to scrutinize:** §3 says "now, not soon," so every slot-3 variant is machine-checked to carry a now-marker and to contain **no** soft-deferral ("as soon as you can" was drafted and caught) — but you should confirm the 999 branch and the see-a-doctor branch read as **clinically distinct** in tone, since §5 forbids naming *why*, so the distinction lives only in urgency wording.
- **Slot 1 → derive slot 5:** ratify one distress-question wording; the gentle re-ask is the same question, so sign it once and we derive the re-ask rather than you picking twice.
- **Placeholder resolution is part of your signature:** `{{crisis_emergency}}` resolves to **999** — this is the *psychiatric-crisis* pathway, deliberately distinct from the *medical* terminal's **998** (ambulance); both numbers are correct for their terminal. `{{first_name}}` is name-only (no content is ever echoed back — reflecting the user's words is the §5 validate-the-delusion hazard). **Changing what a placeholder resolves to is a clinical-copy change requiring re-sign.**
- **The pool is closed by your signature:** the ratified set is content-hash-pinned in CI, so adding or editing a variant later fails the build until it is re-ratified — no unsigned string can reach a user silently.

## Flip-gate condition (Stage 2) — the safety class flips in one voice
When Stage 2's warm variants go live, they go live as a **set** across the safety-exit class. **HR's flip PR must not ship warm-HR beside stiff crisis/medical copy** — voice discontinuity at the worst moment reads as abandonment. This is a **checklist condition on the HR flip PR**, not a note here: crisis and medical get the same voice-pass, ratified in this same review, and the three flip together (or HR after them), never HR first-and-alone.

## Second ask (unblocks the Arabic measurement, routed here so it lands in one conversation)
The Arabic/Khaleeji coverage for these HR triggers — and for the medical red-flag, venting, and unsigned SK-AR rules — is **zero** today, and the plan to measure it (the AR conformance probe) has one hard dependency only you can supply: **who is the native-Khaleeji validator?** Corpus labels are clinical assertions, so the validator must be clinically credentialed and a native Gulf-Arabic speaker; engineering cannot source or assess that credential. **Please name the validator.** No validator, no Arabic corpus, no probe — this is the single blocker on the entire AR measurement track, so naming them now starts it.

## Third ask — a separate psychoed content question (unrelated to HR, routed here to save a meeting)
A conformance measurement found two psychoed categories where the doc prescribes a teaching (psychoed) skill but the skill library's nearest semantic match is **out-of-family**: **§7c**'s best real match is `interpersonal_effectiveness` (not a psychoed skill at all), and **§4a** matches its prescribed skill only 1 of 5 times. This is a **content question, not a routing bug** — either the intended skill's description misdescribes its clinician-authored content, or the skill the doc prescribes doesn't exist in the library as authored. **We will not rewrite a clinical `semantic_description` to make the router match** — tuning clinical content to satisfy the machine is exactly the error this project guards against. So: for §4a and §7c, **confirm the intended skill exists as authored, amend its description, or amend the doc.** (One-line-per-category; the raw match data is attached.) **§3c is a milder instance of the same question:** with the routing fixed, 4 of its 5 drives reach the prescribed skill; the one miss is a phrasing that matches a different library skill — same prescribed-skill-vs-library-content call, lower stakes. Same three options.

**For your visibility (doc-derived, not a new clinical choice):** as part of fixing the same measurement, a psychoed *question* ("what is anxiety/depression?") classified as an info-request can now reach these existing skills — **`psychoed_anxiety`, `psychoed_depression`, `assertive_communication`, `grief_loss`** — instead of always falling to a generic knowledge-base answer. These are exactly the skills the doc already prescribes for §1f/§6d/§3c/S2c; nothing new is authored, but you now see which skills an info-question can surface. Flag if any of these should NOT be reachable from an info-intent turn.
