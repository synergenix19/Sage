# Vee — one tick: activate the deterministic rule that realizes your own item-3 ruling (2026-07-31)

**You already ruled the disposition.** Item-3 (2026-07-30): the *"going to die + can't breathe"* class
stays at crisis — no downgrade without a clean screen. This is not a new question; it is the activation
of the mechanism that makes your ruling unconditional.

**The measured gap (why this is same-day, not calendar):** N=20 fresh-session drives on prod, three
phrasings of the class. **Crisis card 13/20. Empathic freeflow with NO crisis resources — no 999, no
helpline — 6/20.** All six no-resource outcomes were the demonstrated phrasing itself, which had measured
3/3 crisis a few hours earlier: the LLM classifier's routing of this class **flips across time windows**
(the pins do not determinize it), so no clean measurement bounds the next window. Against the ≥95%
crisis-recall KPI this phrasing measured 3/9 in its failing window.

**What is built (INERT, awaiting only your tick):** a deterministic Node-1 rule — death-fear AND
air-hunger co-occurring → crisis flag at safety_check, **before any LLM sees the turn**. Window-independent.
Its term sets are panic_override's own (imported, single-sourced), so the override's deference and this
escalation can never drift apart. Both directions are fixture-held: all three characterized variants
escalate; pure panic without death-fear still grounds (your signed §1c-A capability untouched); death-fear
without air-hunger unchanged. Registry-declared; kill-switch flag, default OFF, instant-off rollback.

> ▢ **Activate** `SAGE_CARDIAC_ESCALATION` (your item-3 ruling, made unconditional) ▢ edit/discuss

On your tick: register PR (signed_value + this record as signature_ref) → apply → live behavioral probe
(all three variants → crisis card; pure-panic control → grounding) → guarded re-measure, flag-stamped.
