# Contraindication-delivery characterization — census + window-1 finding + plan (2026-08-06)

Sweep row 14: signed per-step contraindication strings honored only by compose-time LLM discretion —
the cardiac shape. Characterize before building (measurement proposes, mechanism disposes).

## Census
**28 skills carry 93 per-step contraindication strings** (prose "Do NOT proceed if…" instructions the
executor LLM reads). Acute cluster: dbt_tipp (cardiac/pacemaker/pregnancy/disordered-eating vs cold +
intense exercise), safe_place_visualization (no-safe-place / threatening imagery), box_breathing
(breathing conditions), grief_loss, psychotic_referral.

## Window-1 finding (2026-08-06 14:50Z, prod, purged): the ACUTE ENTRY door is deterministically covered
Probe: agitation + "I have a heart condition" → intense-technique ask. Served: **D1 medical screen fired
at entry** (signed A1 question), and the disclosure took the signed A2 `contraindication_disclosed →
grounding` branch — **TIPP was never delivered; grounding served.** The highest-stakes contraindication
class (cardiac × cold/exercise) is NOT LLM-carried at skill entry: the deterministic screen owns it.

## What remains genuinely LLM-carried (the characterization matrix, next window(s))
1. **Mid-skill disclosure**: contraindication surfaced AFTER the skill starts (screen already passed) —
   does the executor honor the step's "Do NOT proceed"? (The SF-2-adjacent door.)
2. **Classes outside D1's lexicon**: disordered eating (TIPP ice), Raynaud's, asthma (box_breathing
   ratio), no-safe-place (SPV) — the screen never fires; only the prose instruction guards.
3. **Screen-less entries**: skills reached by keyword/offer without medical language in the turn — is
   D1 even consulted?
Each cell: contraindicated-disclosure drive vs control, response-marker assertion (adaptation/deferral
present vs contraindicated content served), two windows. Output ranks cells by clinical stakes for the
gate decision (the #218 ERP pattern generalized to output_gate checks is the candidate mechanism —
Vee rules after the numbers exist).
