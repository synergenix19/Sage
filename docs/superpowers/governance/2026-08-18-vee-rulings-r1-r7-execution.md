# Vee rulings R1–R7 (packet 2) — execution record (2026-08-18)

**Source:** Vee's feedback on packet 2 (PR #460), relayed by the owner 2026-08-18.
**Status legend:** EXECUTED (landed, SHA cited) · SCAFFOLDED (awaiting her named input at
signing) · RECORDED (design premise; build follows its own cycle).

## R1 — CD1 tiers by diagnostic specificity × cost-of-miss — EXECUTED (`279fbc71`, feat)
Axis ratified and encoded (strong: `anhedonia_interest`, `energy_effort` — PHQ-2 core
mirror; weak: `social_withdrawal`, `motivation`). The 7b-overlap rule applied
**mechanically**: `meaning_going_through_motions` tiers STRONG — "everything feels
pointless" overlaps the VG-01 purposelessness class in `passive_si_patterns.json`
(C-SSRS item-1 register), evidence cited in-field. Remaining two carry proposals under her
two questions, flagged for her tick at signing: `affective_flatness` → proposed strong
(cost-of-miss high: flat affect masks distress; flagged least-certain),
`explicit_ask` → proposed weak (low specificity, low miss-cost).

## R2 — signature carries version/date/review-by trigger — SCAFFOLDED (`279fbc71`)
`_signature_block` in v2 with the review-by trigger she sets at signing. **Class rule
recorded:** all her signed artifacts going forward carry it — an oracle cannot silently
outlive the clinical picture it encodes.

## R3 — precision gate as two-stage screening — RECORDED as the F5 design premise (conditional)
Stage 1 (detector): sensitive, holds ≥0.90 recall. Stage 2 (her §3a preliminary
questions — duration, scope, context): specific; deterministic exclusion rules on
transience markers, mildness qualifiers, situational attribution — authored by her as
Rules Service JSON (signed = executed). The zero-tolerance boundary relocates to where it
clinically belongs: **no woven-SI question reaches a transient presenter**; the
detector-level ≤0.00 bar is retired as the screening-practice misconfiguration it was.
**Condition:** her confirmation of the flow property (a false-fire exits at the
preliminary stage). **Fallback if unconfirmed:** she sets an explicit detector-level FP
tolerance she can live with. The new semantic FP on the baseline of record ("feeling a
bit down after a rough week", 0.7629) is exactly the case stage 2 exists to exit.

## R4 — third-party deference: distinct helper-support path — RECORDED; next dark build
Content spec (hers, verbatim intent): validate the helper's concern; gatekeeper guidance
(ask directly — this does not plant the idea; listen without fixing; help connect to
support); 800-HOPE framed as a resource for the friend; one brief check on the helper's
own state. **Two hard boundaries:** the first-person crisis script never serves this case;
deference applies ONLY on Layer-1-clean turns — co-occurring first-person SI fires
deterministically (F1), so her content need not hedge it. Build shape: flag-gated
(`SAGE_THIRD_PARTY_DEFERENCE`, register row at birth, hard-OFF), content as Rules Service
JSON drafted from this spec for her signature, S2a-deference mechanical pattern, both
boundaries as tests. Rides its own PR + decision request per the house cycle.

## R5 — item-1 shortened wording — TICK LANDED
The standing merge guard is lifted: the shortened `_meta.match` (already on the feat
branch) may ride the branch's future merge. Acceptance chain: packet 1 approve-or-edit →
owner shortening (strict subset) → this tick.

## R6 — same-sitting SF-1 Gulf Arabic capture — SCAFFOLDED (`279fbc71`)
`sf1_gulf_arabic_passive_si` slot in v2 ("ابغى اختفي" class), authored by her at the v2
signing; one cycle into the signed record; feeds the F5 regression set directly.

## R7 — sequencing rule — ALREADY MECHANICAL, listed for visibility
v2 signature → v2 baseline on the pre-F5 detector before any rebuild is measured; no
cross-oracle comparison; enforced by the harness's version-stamp and unsigned-abort.

## What returns to her at the v2 signing (one sitting)
1. Tick or amend the two proposed CD1 assignments (R1). 2. Set the review-by trigger
(R2). 3. Confirm the R3 flow property (or set the fallback tolerance). 4. Author the
Gulf Arabic passive-SI phrases (R6). 5. Sign the block. R4's content JSON arrives as its
own draft-for-signature when the dark build lands.
