# #324 / A3 ruling — clinician V, 2026-07-17 (PRIMARY RECORD)

**SUPERSEDES** the "A3 approved as-sent" line in `2026-07-16-safety-relay-signoff-V.md`. That approval
was given without the cross-reference the **bucket-lock guardrail** then surfaced: A3's 6 phrases were
deliberately routed to grounding by the **signed 2026-06-13 C1 decision (B.2/B.3)**, which V's C1 ruling
reaffirmed. Held at the gate-stop, reframed, re-ruled here on corrected premises (#342 retracted — the
SG-2 caveat is bilingual-reliable).

## Ruling: **(a) KEEP GROUNDING-FIRST** (V, 2026-07-17)

The 6 acute-overwhelm phrases (`need to calm down fast`, `I need to calm down`, `مشاعري أقوى من قدرتي`,
`محتاج أهدى بسرعة`, `مشاعري أقوى مني`, `مشاعري فوق طاقتي`) **remain routed to grounding.** Reaffirms
2026-06-13 + C1.

**Rationale (V):** the C1 revisit-trigger's condition ("reconsider TIPP-leads once a contraindication
screen is **robust**") is partially met at best — SG-2's caveat *informs* a cardiac-condition user
mid-delivery but **gates nothing**, and the D1 screen isn't built (#338). Under an incomplete screen the
precautionary posture is **routing-side**: prefer the contraindication-free skill (grounding — the doc
ranks it effective-if-not-maximal for acute overwhelm) over the contraindicated-for-some skill (TIPP:
cardiac/pregnancy, spec L194). Cost asymmetry: a slightly-less-optimal-but-safe technique vs
ice-water/intense-exercise instructions reaching a heart condition — the same asymmetry that ruled every
veto this month.

## Disposition (cleanly severable, correctly severed)
1. **A3 removals DIE.** The 6 phrases stay in grounding. **#324 as-authored does NOT merge** (its removals
   reverse a signed decision). The **bucket-lock test stays intact** (it did its job).
2. **A3's dbt_tipp additions are SALVAGED** as a separate minimal PR — the **severable, non-colliding**
   acute triggers only (`shock my system`, `shock your system`, `emotions out of control`) that strengthen
   TIPP's *own* recognition. The grounding bucket is untouched; the 3 AR overwhelm phrases are NOT added to
   dbt_tipp (they stay grounding; the C1 tiebreak would route them to grounding regardless). This closes
   #311's real "TIPP under-anchored" concern (p2 "shock my system" → TIPP) while p1 (acute overwhelm) stays
   grounding-first — the correct severed outcome.
3. **Revisit trigger (concrete, not a burial):** when **D1 ships and verifies**, the trigger fires properly
   → (b)/TIPP-leads becomes the honest re-question. This is a HOLD until the screen is real.

## D1 build-shape refinement (V, 2026-07-17)
The D1 screening question must NOT be a clinical-intake questionnaire bolted into a warm conversation
(violates the bot-behaviour register: safety woven naturally, never led-with, one step at a time, no
clinical-terminology dumps). The signed shape: **the question is conversational; the consequence is
deterministic.** Falcon renders the ask naturally in flow (e.g. *"before we try this one — it gets the
heart rate up briefly; any heart condition or pregnancy I should know about?"*); the **answer-routing is
deterministic** (spec L105 branches → medical guard), no LLM discretion in the safety consequence. Folds
into the #338 build spec. (Note: the discriminating *content* must still be present in Falcon's rendering —
the #338 harness assertion was tightened tonight to require the markers, not a bare echo.)

Refs #324 #311 #338 #342. Supersedes A3 line in 2026-07-16-safety-relay-signoff-V.md.
