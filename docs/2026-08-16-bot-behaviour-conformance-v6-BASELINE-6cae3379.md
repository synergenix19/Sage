# Conformance baseline v6 — THE BASELINE OF RECORD (prod 6cae3379, 2026-08-16)

**EN: 11/36 categories CONFORM** (strict criterion: all 5 utterances per category), measured
by `measure_layer1_prod_http.py` — production itself over HTTP, serving flags stamped,
the sanctioned instrument of record (local full-graph runs are non-authoritative per the
live-unpinned-classifier guard). **AR: UNMEASURED** (corpus is 100% EN; gated on the
ratified Khaleeji corpus — the same Vee-validator lane). This retires v5's 8/36
(1f687c57, 2026-07-23), which had been cited past its validity.

**Ruling context (header rider per the conformance owner):** measured under the
2026-08-16 §1a bare-affect ruling (`2026-08-16-1a-bare-affect-ruling.md`, Reading A:
direct-offer conformant this phase) and the 2026-08-06 S2a `accept_offer_form` ruling
(honored by the instrument — S2a scores 5/5: 2 presence + 3 offer-form).

## Reading the number honestly

11/36 under a STRICT all-5 criterion. Nine further categories sit at 4/5 — one utterance
off. The month's shipped work is all visible at row level: §3a/§3b (K1/K2) 5/5, §6b (K3)
5/5, §7b (K4) 5/5, §1f/§6d (consult set) 5/5, §7a/§3d presence rows 5/5, S2a 5/5 under
its ruling. The remainder concentrates in the known open classes: §4a (Mechanism-B) 0/5,
§7c 0/5, §5a/§5b (structured-skill reach), §6c (reverted margin-burn attempt), S4x/S5a
rows, and the §1a bare-affect row (1/5, conformant-as-ruled deviation candidate on the
Vee sheet).

## Instrument findings this measurement produced (both fixed/recorded)

1. The local runner ignored the merged `accept_offer_form` ruling (data-present,
   logic-ignored; fixed #445 — both instruments now honor it identically).
2. Local-vs-prod row-level divergence is REAL (e.g. S2c 4/5 local vs 0/5 prod; S2b 3v1;
   §1d 5v4): the live-unpinned local classifier measures a different system. The
   prod-HTTP number is the only citable one; local runs are diagnostic-only, as the
   guard enforces.

## Variance note

Single prod run under strict criterion; the 4/5 band (9 categories) is the flip-sensitive
zone — treat single-row movements in future re-runs as within-band until an N-sample
characterization exists for the near-miss rows. Deltas vs this baseline should be read at
row level, not headline level.
